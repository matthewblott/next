{
  "type": "module",
  "scripts": {
    "build": "rm -rf www && pnpm eleventy --output=www",
    "index": "pnpm pagefind --site www"
  },
  "devDependencies": {
    "@11ty/eleventy": "3.0.0-beta.1",
    "@11ty/eleventy-img": "^5.0.0-beta.10",
    "markdown-it": "^14.1.0",
    "markdown-it-attrs": "4.2.0",
    "pagefind": "^1.1.0"
  }
}
